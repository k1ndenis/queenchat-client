# Copy this file to android/keystore.properties (ignored by Git), or put the
# same keys in ~/.gradle/gradle.properties / environment variables.
#
# Do not commit the real values. Use an absolute local path; Gradle does not
# expand $HOME inside this properties file.
QUEENCHAT_KEYSTORE_PATH=
QUEENCHAT_KEYSTORE_PASSWORD=
QUEENCHAT_KEY_ALIAS=
QUEENCHAT_KEY_PASSWORD=
